apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: storage-gateway
spec:
  selector:
    istio: istio-ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"